name: oronbox
description: "A pretty fast wearable management tool for VelaOS and ZeppOS, built with Flutter."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.3+15

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  # Pinned to 0.20.2 to match flutter_localizations in this Flutter fork (3.41.9-oh).
  intl: 0.20.2
  go_router: ^17.3.0
  flutter_riverpod: ">=2.5.0 <3.0.0"
  url_launcher: ^6.3.1
  cached_network_image: ^3.4.1
  # dynamic_color 2.0+ adopted package:material_ui which requires Dart 3.12+.
  # 1.9.0 was the last release before that migration.
  dynamic_color: ">=1.7.0 <2.0.0"
  # material_ui (Flutter Material split-out package) requires Dart 3.12+,
  # but our Flutter fork ships Dart 3.11.5. We import the built-in
  # package:flutter/material.dart directly instead — the API surface is
  # identical because material_ui is literally that file extracted.
  dio: ^5.8.0
  csv: ^8.0.0
  html: ^0.15.6

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  freezed_annotation: ^2.4.0
  json_annotation: ">=4.0.0 <4.12.0"
  segmented_list:
    path: packages/segmented_list
  flutter_svg: ^2.3.0
  shared_preferences: ^2.5.5
  universal_ble: ">=1.0.0 <2.0.0"
  protobuf: ^6.0.0
  fixnum: ^1.1.1
  pointycastle: ^4.0.0
  crypto: ^3.0.6
  file_picker: ^12.0.0
  cross_file: ^0.3.5+2
  archive: ^4.0.6
  image: ^4.5.4
  charset: ^2.0.1
  enough_convert: ^1.6.0
  logging: ^1.3.0
  qr_flutter: ^4.1.0
  app_links: ">=6.3.0 <7.0.0"
  path_provider: ^2.1.6
  desktop_drop: ^0.8.0
  window_manager: ^0.5.2
  tray_manager: ^0.5.3
  flutter_soloud: ^4.0.12
  # Keep the prebuilt-library plugin path. wasm_run 0.2 currently emits
  # dynamic Android libraries with a static `.a` filename under Flutter 3.47.
  wasm_run_flutter: ^0.1.0
  quickjs_engine: ^0.1.5
  # Temporarily swapped from the upstream git dependency to a local path
  # while we are offline (Pub's git ref fetch requires GitHub connectivity).
  # The path points at a v0.1.2 mirror cloned locally from Pub's cache.
  oronbox_network:
  oronbox_network:
    git:
      url: https://github.com/zxor-org/OronBox-Package-Network.git
      ref: v0.1.3
  web: ^1.1.1
  flutter_markdown_plus: ^1.0.12
  uuid: ^4.5.3
  mdns_dart: ^2.2.2
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  # build_runner 2.15.2+ pulls analyzer 13+ which needs meta 1.18+,
  # but our Flutter fork pins meta at 1.17.0.
  build_runner: ">=2.4.0 <2.15.2"
  freezed: ^2.5.0
  # json_serializable 6.15.1+ pulls in build 4.x which clashes with freezed 2.x.
  json_serializable: ">=6.0.0 <6.15.1"

dependency_overrides:
  # wasm_run_flutter 0.1.0 still declares the Freezed 2 annotation constraint,
  # while its generated bindings remain compatible with the Freezed 3 runtime.
  freezed_annotation: ^2.4.0
  # jni 1.0.3 dropped the __ENVP_CAST workaround and fails to compile dartjni
  # on desktop with recent clang.
  jni: 1.0.0

flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/team/
    - assets/images/devices/
    - assets/scripts/zeppos/zml.js
    - assets/images/brands/
    - assets/licenses/
    - assets/wasm/
    - assets/legal/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
