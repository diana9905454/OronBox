# OronBox 鸿蒙版上架清单（华为应用市场 / 鸿蒙激励计划 2026）

> 生成时间：2026-08-30
> 目标：把 OronBox（`org.zxor.oronbox`）上架华为应用市场，达成「鸿蒙激励计划 2026」的领奖条件。
> 当前进度：Release `.app` 已构建成功（`build/ohos/app/ohos-default-signed.app`，版本 1.1.2，vendor=欧阳瀚予），但仍是 **debug 证书签名，不能直接上架**，需换发布证书重签。

---

## 一、先看全局（三条线并行，别串行等）

```
┌─────────────────────────────────────────────────────────────┐
│  报名激励计划（9.25 截止）   +   商户服务开通（9.30 截止）      │
└─────────────────────────────────────────────────────────────┘
          │
          ├── 线 A：资质合规（时间最长，立刻启动）
          │     ├─ 开发者账号实名认证（个人/企业）
          │     ├─ APP 备案（联网应用硬性准入，10~20 工作日）
          │     └─ 电子版权认证（建议办，10~15 工作日）
          │
          ├── 线 B：签名与打包（最快，1 天内可完成）
          │     ├─ 生成 .p12 + .csr
          │     ├─ AGC 换发布证书 .cer
          │     ├─ 建发布 Profile .p7b
          │     └─ 重签 → 构建 Release .app
          │
          └── 线 C：AGC 建应用 + 上传审核（1~7 工作日）
                ├─ 建应用（包名 org.zxor.oronbox，永久锁定）
                ├─ 填应用信息 + 素材
                ├─ 上传 .app
                └─ 提交审核
```

**时间最紧的是线 A 的 APP 备案**——它不能省，且周期最长，请最先启动。

---

## 二、线 A：资质合规（硬门槛，缺一不可）

### A1. 开发者账号实名认证
- 登录 [华为开发者联盟](https://developer.huawei.com/)，完成**个人**或**企业**实名认证。
- **个人开发者**：提交真实姓名 + 身份证号。
- **企业开发者**：营业执照（统一社会信用代码）+ 法人身份证 + 对公账户。
- ⚠️ 报名激励计划的账号 **必须 = 上架账号**，别用错号。

### A2. APP 备案（联网应用硬性准入，最优先启动）
> 依据《移动互联网应用程序备案工作的通知》，未备案的联网应用 **不得上架、系统直接拦截**。

- 入口：任选一家云服务商（**华为云 / 阿里云 / 腾讯云**）的 ICP 备案通道。
- 填写：主体信息（个人填真实姓名 + 身份证号，**不是** vendor/团队名）+ APP 信息（应用名「OronBox」、平台类型勾选「鸿蒙」、包名 `org.zxor.oronbox`）。
- 若已有安卓/iOS 备案、新增鸿蒙版：走**变更备案**，在原记录上**新增勾选「鸿蒙」平台 + 鸿蒙包名**，不能直接复用。
- 流程：接入商初审（1~3 天）→ 工信部短信核验（24 小时内点链接）→ 管局终审（个人约 7~20 天）。
- 拿到备案号（如 `粤ICP备XXXXXXXX号-X`）后回填 AGC「备案信息」。

### A3. 软件著作权 / 电子版权（建议办，非强制）
- **2026-06-01 起，软著对工具类应用是「非必选资质」**，但强烈建议办「**电子版权认证证书**」（认监委体系机构出具，10~15 工作日，比传统软著 30 工作日快很多）。
- 作用：① 应用名占位防抢注；② 应对侵权投诉；③ 游戏/内容类才强制。
- 上传入口：AGC → APP与元服务 → 应用名 → 版本信息 → 版权信息。
- ⚠️ 软著/电子版权上的**软件名称、著作权人主体**必须与上架应用名、开发者账号主体**三方完全一致**。

---

## 三、线 B：签名与打包（发布证书 + 发布 Profile）

> 现在这个 `.app` 是 **debugKey 调试证书**签的，应用市场不认。必须换成「发布证书」重签。

### B1. 生成密钥库 .p12 + 证书请求 .csr（DevEco 中文版）
1. 顶部菜单 → **构建** → **生成密钥和CSR**（Generate Key and CSR，可能仍是英文）。
2. 密钥库文件（Key Store File）点 **New（新建）**，选路径存 `.p12`，如 `D:\oronbox-release\release.p12`。
3. 密码：至少 8 位，大小写+数字+特殊符号选两种以上。**务必记住**。
4. 别名（Alias）：如 `oronbox`。有效期（Validity）填 10000 天以上。
5. 证书信息**不能用中文**：姓名、组织、城市、省份、国家码（填 `CN`）。
6. 最后设 `.csr` 保存路径 → **完成**。得到 `release.p12` + `release.csr`。

### B2. AGC 申请发布证书 .cer
1. 登录 [AppGallery Connect](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html)。
2. 左侧导航 → **证书、APP ID和Profile** → **证书** → **新增证书**。
3. 证书名称自定义；证书类型选 **发布证书**；上传 B1 的 `.csr` → 提交。
4. 下载生成的 **`.cer`**。（每个账号最多 3 张发布证书，实名开发者有效期 3 年。）

### B3. AGC 建发布 Profile .p7b
1. 左侧导航 → **证书、APP ID和Profile** → **Profile** → **添加**。
2. 类型选 **发布**，绑定 B2 的发布证书 + 包名 `org.zxor.oronbox`。
3. 提交 → 下载 **`.p7b`**。
4. ⚠️ 申请发布 Profile 时，AGC 会自动把发布证书的指纹加到应用；备案用的公钥+指纹也从**发布证书**（不是调试证书）提取。

### B4. 配置签名 + 重新构建
1. DevEco → **文件** → **项目结构** → **Project** → **签名配置（Signing Configs）**，取消勾选「自动生成签名」。
2. 填入：
   | 字段 | 内容 |
   |---|---|
   | Store File | B1 的 `.p12` |
   | Store Password | B1 设的密码 |
   | Key Alias | B1 设的别名 |
   | Key Password | 同 Store Password |
   | Sign Alg | `SHA256withECDSA` |
   | Profile File | B3 的 `.p7b` |
   | Certpath File | B2 的 `.cer` |
3. 顶部菜单 → **构建** → **构建 Hap(s)/APP(s)** → **构建 APP(s)**。
4. 或走云端重签（更省事）：**构建 → 上传产品（Upload Product）**，让华为云端证书自动重签并上传。

---

## 四、线 C：AGC 建应用 + 上传审核

### C1. 建应用（包名永久锁定，仔细核对）
1. AGC → **我的项目** → 新建项目 → **添加应用**。
2. 平台选 **APP(HarmonyOS)**，软件包类型 **APP(HarmonyOS 应用)**（非元服务）。
3. 包名填 `org.zxor.oronbox`（**创建后不可修改**）。
4. 分类选「工具」；支持设备选「手机」；默认语言「中文（简体）」。
5. ⚠️ 2026 年起华为收紧建应用权限，若新建账号无「应用创建」入口，需邮件 `agconnect@huawei.com` 提供开发者 ID 申请加入受邀名单（3~5 工作日）。

### C2. 填应用信息 + 素材
| 项 | 要求 |
|---|---|
| 应用图标 | 512×512 PNG（源图 1024×1024 导出，无透明通道，不用系统图标） |
| 应用截图 | 至少 3 张，建议 5~8 张，1080×2340（9:16）竖屏，**真实界面**，无第三方品牌、无 debug 信息 |
| 应用名称 | 「OronBox」，与备案、软著**三方一致** |
| 一句话简介 | ≤30 字 |
| 详细描述 | ≥100 字，说明手环同步工具用途 |
| 隐私政策链接 | **必填**，独立网页 URL，能正常打开 |
| 备案号 | 回填 A2 拿到的 ICP 备案号 |

### C3. 上传 + 提交审核
1. 上传 B4 产出的发布签名 `.app`。
2. 提交审核（1~7 工作日）。

---

## 五、审核高频被拒点（提前自查）

1. **隐私合规（最高频）**：必须有可访问的隐私政策页；应用**首次启动必须弹窗**让用户主动同意隐私政策，未同意不得采集任何数据；申请了哪些权限，隐私政策里就要解释为什么需要。
2. **权限过度申请**：`module.json5` 只保留必要权限。OronBox 当前申请的 BLE/定位/网络/后台保活都要有对应使用场景（权限 reason 文案已规范化 ✅）。
3. **功能不完整**：不能有"敬请期待"空页面或空入口。
4. **图标/截图不合规**：图标模糊、截图含第三方品牌、与当前版本不符。
5. **调试残留**：移除 `console.log`、测试接口。

---

## 六、领奖条件提醒（激励计划 2026）

- OronBox 走 **「新应用」档，3000 元/个**：任一自然月**有效月活 ≥400** + **评分 >3 且评分数 ≥10**。
- 设备范围限手机/平板/PC（手机端符合）。
- 首次领激励后须保持在架至 **2026-12-31**。
- 不可与耀星/鸿飞/创新赛同享；单主体累计上限 100 万。

---

## 七、待办状态（实时）

- [x] Release `.app` 构建成功（版本 1.1.2，debug 签名）
- [x] 权限 reason 文案规范化（三语言）
- [x] vendor 名称（已填 `欧阳瀚予`，2026-08-30）
- [ ] 开发者账号实名认证
- [ ] APP 备案（**最先启动**）
- [ ] 电子版权认证（建议）
- [ ] 发布证书 + 发布 Profile
- [ ] 重签构建发布 `.app`
- [ ] AGC 建应用 + 上传 + 提交审核
- [ ] 报名激励计划（9.25 前）+ 开通商户服务（9.30 前）
